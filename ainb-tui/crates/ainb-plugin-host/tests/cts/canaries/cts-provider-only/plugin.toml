[plugin]
name = "cts-5d2c81f6-provider-only"
version = "0.0.1"
ainb_min_version = "1.0.0"
description = "CTS canary: declares a `provides.providers` entry, no UI."

[capabilities]

[provides]
providers = ["cts-noop-provider"]
