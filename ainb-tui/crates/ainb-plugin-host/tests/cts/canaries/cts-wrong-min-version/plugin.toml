[plugin]
name = "cts-83b4011f-wrong-min-version"
version = "0.0.1"
# Host advertises 1.1.0; this plugin demands a host that can't exist yet.
ainb_min_version = "999.0.0"
description = "CTS canary: requires a host version newer than this build."

[capabilities]

[provides]
