[plugin]
name = "cts-a07c4b91-malformed-buffer"
version = "0.0.1"
ainb_min_version = "1.0.0"
description = "CTS canary: emits a WireBuffer whose cells.len() != width*height."

[capabilities]

[provides]
sidebar = ["cts-a07c4b91-sidebar"]
