# Amendments to docs/plans/2026-09-11-multi-surface-decisions-spec.md

Critique date: 2026-09-11. Decisions D1-D18 are treated as locked; every row below is a spec change that handles a consequence, not a different decision. Verdict: CAUTION, confidence 8/10.

Code facts verified for this critique (file:line at HEAD 95209f38): `rpc/mod.rs:240` `bind()` returns a concrete `UnixListener`; `rpc/mod.rs:352` `serve_conn(stream: UnixStream)` calls `auth::classify_peer` (there is no `handle_conn`); `proto/fleet.rs:14` `FLEET_PROTOCOL_VERSION = 2` fleet-only and `proto/auth.rs:43-47` `HelloParams { token }` carries no version; `proto/fleet.rs:50-61` capabilities are advisory and `rpc/mod.rs:1492-1517` negotiate is a stateless echo; `answer.rs:71-180,788` claim via `mark_answered_if_open` then `reopen_on_failed_delivery`, `PICKER_SETTLE` 2000 ms; `proto/fleet.rs:336-400` and `migrations/0044` have no host column, `fleet-core/src/fleet/types.rs:69-77` `SessionKey::legacy` is `legacy:{provider}:{tmux_target}:{fingerprint}`; `migrations/0044:88` `fleet_action_receipt(request_id, expected_version, idempotency_key, status)` already exists; `migrations/0024:47` `event_log.seq` and `0044:70` `fleet_event.revision` are two durable cursors; `ainb-web/src/terminal.rs:288-330` PtyBridge runs `tmux attach-session` at 80x24 with a 256-chunk `blocking_send` channel; `hangar-client/src/lib.rs:599,695,739` three hardcoded `UnixStream::connect` sites and "ordinary calls use a fresh connection"; `events.rs:38` broadcast capacity 256 lossy, `event_outbox.rs` loss window on hard crash between emit and drain; `attention_ingest.rs` 3 s tick, 300 s sweep, `att:<session>:<event_id>` idempotency; `tmux -V` on this box is 3.4 and no crate checks a tmux version floor; `route.rs:25` `Transport` is a private, process-global env-selected enum; P0 plan Phase S-B edits `HelloParams`, `rpc/mod.rs`, `rpc/auth.rs`, `answer.rs`, `hangar-client` (the same files W0 and R1 edit) and Phase 2 hardcodes `SectionVersions = [u64; 19]` with no `agent_status` section.

Table cells that carry a spec table row use `\|` for the inner pipes; unescape on paste.

| # | Spec section | Gap | Exact amendment text | Severity |
|---|---|---|---|---|
| 1 | Decisions D11, Open questions | `HostId` is on every wire type and schema row but its identity source is an open question. Hostname-derived ids collide on reinstall and clone; a box restored from backup must keep its identity, a fresh install must not | Replace the open question with: `- [x] HostId: a ULID minted once at first daemon boot, persisted in hangar.db (`daemon_identity` table: host_id, host_static_pubkey, display_name, created_at). Never derived from hostname. Backup restore keeps the id; fresh install mints a new one. A client that holds a pairing whose host_id matches but whose pinned static key differs refuses with `peer_changed` and requires re-pair. Display name is client-side and renameable; the wire never carries it. Existing `fleet_session` rows migrate with `host_id = <this daemon's id>`.` | critical |
| 2 | Decisions D11 | Two boxes produce identical `session_key` values (`legacy:claude:main:0:...` is box-local). Palette, notifications, deep links and mobile routes need a cross-host reference | Add to D11 locked answer: `every cross-host surface (palette, OS notification payload, deep link, mobile route, `answered_by`) carries `SessionRef { host_id, session_key }`; a bare `session_key` is a type error outside a single-host `HostApp`. Proto gains `SessionRef`; tripwire asserts no `String` session key in `ainb-desktop` shell types.` | major |
| 3 | Decisions D13, Security requirements | "scope allowlist" is named but no scope exists. Subscriptions leak: a narrow scope on `workspace/subscribe` receives every event family. Interactive terminal from a phone is a shell on the box and needs its own opt-in | Add a Scopes table under Security requirements: `\| Scope \| May call \| May receive \| Notes \|` `\| operator \| everything \| everything \| local unix leg only (SO_PEERCRED) \|` `\| desktop \| everything except device admin, daemon stop, transcript prune \| everything \| laptop pairing default \|` `\| mobile \| fleet/snapshot, fleet/subscribe, attention/list, attention/answer, fleet/message_send, fleet/action{cancel}, fleet/transcript_*, terminal/attach \| fleet, attention, transcript events only \| phone default \|` `\| mobile+type \| mobile plus terminal/input, terminal/resize \| as mobile \| separate toggle shown on the pairing screen: "this device may type into terminals" \|` `\| admin \| device_list, device_revoke, device_rescope \| ConnectionsChanged \| granted to the first paired desktop; revocable \|` Add rule: `the allowlist filters event families on subscribe as well as methods on dispatch; a scope may never receive an event whose read method it cannot call.` | critical |
| 4 | Decisions D13, Security requirements | A pairing offer that embeds the device token is a bearer credential: replayable within its TTL, shoulder-surfable as a QR, and there is no box-side confirmation | Replace the D13 pairing clause with: `pairing offer = { v, host_id, host_static_pubkey, endpoints[], invite_id, invite_secret, expires_at, relay? }. The offer is single-use: the first Noise IK session that presents `invite_secret` consumes it and receives `{ device_id, device_token, scope, expires_at }` in the pairing reply. TTL 5 min, 30 s skew leeway, 5 attempts then the invite is burned. The device token is bound to the initiator static key of the Noise session that redeemed the invite: presenting it over any other static key is `UNAUTHORIZED` and logged. The desktop shows "paired: <device name>, scope <s>" with a one-click revoke for 60 s after each pairing.` | critical |
| 5 | Decisions D13 | The host static key is pinned by every device but the spec says nothing about where it lives or how it rotates; losing it strands every pairing | Add under Security requirements: `- Host static key: generated at first boot, stored via `ainb-hangar-secrets` (keychain) with a `0600` file fallback under `{home}/hangar/`, backed by the `daemon_identity` row. Rotation: `ainb hangar host-key rotate` mints a new key and a `key_rollover` record signed by the old key; a device accepts the rollover once, on the next connect, and re-pins. Emergency rotation (`--revoke-all`) burns every pairing.` | major |
| 6 | Decisions D13, Security requirements | The WebSocket listener has no bind policy, no pre-auth admission limits, and the existing 256-slot cap drops connections with no close code | Add under Security requirements: `- WS listener bind policy mirrors `ainb-web::check_bind_security`: default bind is the tailnet interface when one exists, else loopback (reach it over `ssh -L`); `--bind <lan-ip>` requires an explicit flag and logs a warning at every boot.` `- Pre-auth limits: first frame within 2 s, 30 handshake attempts per minute per source address, 8 open unauthenticated sockets per source; over-cap accept is closed with WS code 1013 and a `Retry-After` hint, never a silent drop.` `- Close-code taxonomy in proto: 4401 unauthenticated, 4403 revoked, 4409 protocol incompatible, 4429 rate limited, 4503 draining; clients branch on the code (revoked latches "re-pair", never retries).` | major |
| 7 | Decisions D13, D16, Components | D13 picks Noise IK via `snow`, which is Rust; the phone is Hermes JavaScript with no CSPRNG and no `Buffer`. There is no Noise IK implementation story for the phone, and the spec separately wants one pairing parser and no hand-written wire validators in the bundle | Add a Components row: `\| `ainb-wire-mobile` (new) \| Rust crate compiled for iOS and Android via uniffi: Noise IK over the WS frame, pairing offer parser, op-id minting, message framing, keychain-backed device key. The Expo app calls it through a thin TS binding; no crypto and no wire parsing in JavaScript \| new; shares `ainb-hangar-proto` and the `snow` dependency with the daemon \|` Add to D16 locked answer: `transport and crypto live in `ainb-wire-mobile`, not in the JS bundle`. Add spike 5 (see spikes list below). | critical |
| 8 | Decisions D14, Phases T0 | Today two records of "needs input" already drift (`attention` table vs `fleet_session.attention_state`, 732 vs 7 measured, reconciled by a 300 s sweep). T0's "status store tables" would be a third writer unless the store is defined as the `fleet_session` family | Replace "daemon-owned single-writer store" in D14 with: `the status store IS the `fleet_session` + `fleet_event` family, extended with provenance, tier, three clocks and incarnation. `attention` rows become a projection written by the same single-writer apply path in the same transaction; `fleet_session.attention_state` is no longer written by any other producer; `sweep_once` becomes an assertion that logs drift and never mutates.` T0 exit gate gains: `zero rows where `attention.open` disagrees with `fleet_session.attention_state` after a 1,000-event replay fixture`. | critical |
| 9 | Decisions D14 | "three clocks" is named, not defined; mirrored rows on a laptop or phone will compute staleness against a remote clock; a host that is unreachable is a different fact from a row that is silent | Add under D14 a Clocks table: `\| Clock \| Set by \| Used for \|` `\| received_at \| daemon, monotonic per connection watermark \| ordering, replay cut \|` `\| evidence_observed_at \| the source (hook payload ts, OSC frame ts, process exit ts) \| staleness, never moved by a replay \|` `\| state_started_at \| daemon, when `state` last changed \| "working since", attention ordering \|` `\| mirrored_received_at \| the mirroring client, its own clock \| decay on a mirror; a client never subtracts a remote stamp from local now \|` Add: `per-host `reachability { reachable \| unreachable{since} \| stale{last_seq} }` is a HostApp field, never folded into a row's `state`; rows on an unreachable host render frozen with `stale_since`, not `unverifiable`.` | major |
| 10 | Decisions D14 | Hydrate ordering, per-tier spool, duplicate delivery and incarnation fencing are not specified. Tier 0 already has a durable spool (`events.jsonl` byte-cursor replay); tiers 1 and 2 have none | Add under D14: `- Spool: tier 0 = `events.jsonl` (exists, cursor replay, `att:<session>:<event_id>` idempotency); tier 1 = none needed (ACP children die with the daemon and are reclaimed by runtime instance id); tier 2 = none (OSC bytes during daemon downtime are lost; such rows hydrate as `restored_unconfirmed` until the next frame).` `- Boot order: hydrate rows and stamp `restored_unconfirmed` on every non-`exited` row, then drain the tier-0 cursor, then start the feed. A hydrated row is confirmed only by a tier 0/1 event in this daemon incarnation.` `- Fence: every row carries `session_incarnation` (`process_start_fingerprint` for tmux panes, pool session id for ACP); an event whose incarnation differs from the live row is `restart` (new row, old row `exited` only with process proof) or `suppress` (older incarnation). Idempotency key for the store: `(host_id, session_key, tier, event_id)`.` | major |
| 11 | Decisions D14 | A provider CLI upgrade that renames a hook event makes tier 0 silent and the store reports `unverifiable` forever with nothing pointing at the cause. There is no rollback to the pane-regex path if a normalizer ships broken | Add under D14: `- Unknown event names per provider are counted (`status_unknown_event{provider,name}`) and surfaced in `ainb doctor`; a normalizer answers `null` for an unknown name, never an error.` `- Rollback: config `[fleet.status] legacy_classify_primary = true` restores today's `classify()`-first ordering for one release after T0 ships; removed in T0+2.` | major |
| 12 | Decisions D10, Spikes 1 and 2, Phases R2 | The named feed, a rendered `tmux attach` under `portable-pty`, parses tmux's re-render (fidelity risk named in the spike) AND makes the daemon a sizing client: under the S-A `window-size latest` pin the headless attach becomes "latest" on every reattach and resizes the pane under the TUI. tmux control mode (`tmux -C attach`) delivers raw pane bytes (`%output`), needs no pty, is excluded from window sizing, offers per-pane `refresh-client -A` subscribe and `-f pause-after` flow control, and does not depend on `allow-passthrough` because the bytes are pre-parse. A well known terminal app's tmux integration is the existence proof for full-fidelity emulation from `%output` | Rewrite spike 2 as: `\| 2 \| Two feeds side by side: (a) rendered `tmux attach -f ignore-size,read-only` under `portable-pty`, (b) `tmux -C attach` control mode on a pipe with `refresh-client -A %<pane>:on` and `-f pause-after=2`. Drive alt-screen, mouse tracking, wide chars, OSC 8 and a custom OSC status frame; feed a Rust VT emulator; diff snapshots against a direct PTY; measure RSS per emulator at 100 sessions and CPU during `cat` of 50 MB; confirm neither feed changes `#{window_width}` seen by a second attached client \| 2-3 days \| R2 \| picks the feed; poor fidelity on both reopens daemon-owned PTY \|` Rewrite spike 1's flip column: `on failure with feed (a), the in-band tier still works over feed (b); hooks alone only if both fail`. Add to Security requirements: `the daemon's tmux client never influences pane size: `-f ignore-size` on a rendered attach, or control mode`. Add to Open questions: `tmux version floor: `allow-passthrough` needs 3.3, `ignore-size` and `pause-after` need 3.2; the daemon checks `tmux -V` at boot and degrades tier 2 with a doctor warning below the floor.` | critical |
| 13 | Phases R2, Edge cases | The emulator is a chokepoint with no stated failure story: what viewers see when the daemon's feed dies, who reattaches, and what the emulator keeps | Add under R2 contents: `emulator is a cache, tmux is truth: live window capped at 1,000 rows per session (env-tunable 100-5,000); deeper scrollback is served on demand from `capture-pane -e -S -<n>`; on feed loss the daemon reattaches with backoff 250 ms/1 s/4 s, emits `data_gap{reason: feed_lost}` to every viewer, rebuilds the screen from the redraw, and stamps every status row for that session `restored_unconfirmed` until the next tier 0/1 event.` Add edge-case rows: `\| tmux server exits \| every pane process gets SIGHUP; daemon marks rows `exited` only after `kill(pane_pid, 0)` fails; until then `unverifiable`; viewers get `data_gap{reason: session_gone}` and the tab closes with the recovery flow \|` `\| daemon feed dies, tmux alive \| viewers see `data_gap`, re-snapshot within 5 s, no keystroke lost because input goes through `send-keys`, not the feed \|` | major |
| 14 | Phases R2, Edge cases "two typists" | The driver floor only governs viewers that type through the daemon. A TUI embed client or a human `tmux attach` bypasses it, so "two viewers type without interleaving" is only true for daemon-mediated viewers | Reword R2 exit gate: `two daemon-mediated viewers type without interleaving`. Add edge-case row: `\| native tmux client attached while a phone holds the floor \| floor cannot enforce; presence badge shows "native client attached, input not arbitrated" from `tmux list-clients`; the floor owner sees the badge before typing \|` | major |
| 15 | Decisions D18 | Op-id namespace and retention are unspecified. Two devices can mint the same id; a retry after ledger eviction must not become a second execution | Add under D18: `ledger key = (host_id, principal, op_id) where principal is `device:<id>` off-box and `local` on the unix leg; a matching op_id from a different principal is `rejected{reason: op_id_foreign}`. Retention: 7 days or 100k rows per host, whichever first; a retry after eviction returns `unknown{reason: op_expired}` and the client shows "could not confirm, check the session". Op ids are opaque 128-bit values; no timestamp freshness rule, so a phone with a wrong clock is never rejected for skew.` | major |
| 16 | Decisions D18 | "expected fence" is one word for three different mutations. A kill fenced on the wrong thing kills a reused tmux session name | Add a Fences table under D18: `\| Mutation \| Fence \| Stale means \|` `\| attention/answer \| attention row `version` and `state = open` \| already answered: `rejected{already_answered_by}` \|` `\| fleet/message_send, prompt send \| `fleet_session.lifecycle_updated_at` observed by the client \| the agent moved on; `rejected{turn_advanced}` unless `force` \|` `\| fleet/action{cancel, kill} \| `session_incarnation` \| a different process now owns the name: `rejected{incarnation_mismatch}` \|` `\| device_revoke \| registry `version` \| concurrent admin edit: `rejected{conflict}` \|` | major |
| 17 | Decisions D18 | "durable receipt at the pre-PTY-write boundary" does not survive the case the spec exists for: a crash between commit and reply, or between claim and `send-keys`. The outbox drain has a documented crash loss window, so a receipt written through it is not durable | Add under D18: `receipt lifecycle `claimed -> writing -> delivered \| failed \| unknown`, written in the SAME SQLite transaction as the state flip (never via the event outbox). `writing` is set immediately before the first byte reaches the PTY. On boot every receipt still in `writing` becomes `unknown{effects_ambiguous}`; the row is NOT reopened (a retry could double-type) and NOT closed silently: it is surfaced as an attention row of kind `delivery_unconfirmed` naming the answer text, and only an operator closes it. A retry against a `claimed` or `writing` receipt returns `replayed` with the receipt state.` | critical |
| 18 | Decisions D18, Edge cases "two clients answer one ASK" | `adopted` is defined for identical resumes; two clients answering with different text are not the same operation | Add under D18: ``adopted` requires the mutation body fingerprint to match the committed one; a different body against a committed row is `rejected{already_answered_by}`.` Reword the edge case: `same answer twice: `created` then `adopted`; different answers: `created` then `rejected`, `answered_by` names the winner`. | minor |
| 19 | Decisions D18, Components | `fleet/action` already carries `request_id`, `expected_version` and `fleet_action_receipt` (migration 0044). The spec introduces a second idempotency vocabulary without naming the mapping, and "every mutation" is ~60 dispatch arms with hand-written transactions | Add under D18: `existing `FleetActionParams.request_id` IS the op id and `fleet_action_receipt` IS the ledger for that family; W0 renames nothing on the wire and adds `op_id` as an alias accepted on the envelope. Two tiers of guarantee: (1) generic dedupe at dispatch for every mutation, keyed by ledger row storing the serialized reply (`replayed`); (2) transactional receipts with the `writing` boundary only for PTY-effecting mutations: `attention/answer`, prompt send, `fleet/action{cancel,kill}`, `terminal/input`. Adding a handler to tier 2 is a checklist item, not the default.` | major |
| 20 | Decisions D17 | "one integer protocol version" is ambiguous next to `FLEET_PROTOCOL_VERSION = 2` and the 25 fleet capability ids; two integers would be worse than none | Add under D17: ``PROTOCOL_VERSION` lives in `auth/hello`: `{ token, surface?, device?, protocol: {min, max}, capabilities: [..] }` and the reply carries the daemon's `{ protocol, capabilities }`. `FLEET_PROTOCOL_VERSION` is frozen at 2 forever; `fleet/negotiate` stays as a compatibility echo and its 25 ids are appended to the one capability catalogue. Bump rule: the integer bumps only on removing a method or field, changing a field's meaning, or changing framing, auth or crypto; new methods, optional fields and new event kinds are capability strings. The catalogue is a committed file; a test fails if a string is removed.` | major |
| 21 | Decisions D11, D17 | "socket path versioned by protocol" orphans today's clients that hardcode `hangar.sock`, and one box will routinely run two daemon builds (desktop sidecar and brew install) that race the flock singleton | Add under D17: `the daemon binds `hangar.sock` (current) and symlinks `hangar-v<N>.sock` for every protocol version it serves; a client dials the versioned path when it knows one, else `hangar.sock`. The skew harness matrix includes the local leg: {sidecar daemon, installed daemon} x {TUI, desktop, web, CLI, Swift app} at N and N-1.` Add edge-case row: `\| desktop sidecar daemon is older than the installed `ainb` \| flock winner serves; loser's clients negotiate down; a client that cannot negotiate shows "daemon protocol too old, restart from the newer binary" and never spawns a second daemon \|` | major |
| 22 | Components (Swift fleet app, `ainb-mobile`) | "generate `FleetWire.swift` from proto" names no generator; `tauri-specta` emits TypeScript only | Replace the two Reuse cells with: `generated by `typeshare` from `ainb-hangar-proto` (Swift, Kotlin, TypeScript from one annotation set); `tauri-specta` stays for the renderer sections only; CI byte-diffs the generated files.` | minor |
| 23 | Target shape, D15, M1, Open questions | The phone is drawn consuming "sections+stream", but sections are the `ainb-app` renderer contract that lives in the TUI or desktop process; the daemon speaks hangar JSON-RPC. D15's mobile justification ("a phone receives all 19 sections") is wrong, and the open question about a phone's section subset has no referent | Add a Contracts table after Target shape: `\| Contract \| Between \| Selectivity \|` `\| hangar RPC \| daemon and every client incl. the phone \| per-method subscriptions (7 arms today), `HostId`-scoped \|` `\| section mirror \| `ainb-app` and its renderers (ratatui, Tauri Channel) \| D15 subset subscription \|` Change the phone's arrow label to `RPC subs + stream`. Reword the open question to `which RPC subscriptions a phone opens by default (fleet, attention, transcript for the open session only)`. D15's justification becomes the measured renderer fan-out, not the phone. | major |
| 24 | Decisions D16, Phases M1, Do not build | "notifications off the live socket" cannot fire with the app suspended (iOS kills the socket within about 30 s of backgrounding), so the M1 exit gate "answer a needs-input banner in two taps" is only true in the foreground. The options paper's VAPID web-push fallback does not apply to an Expo app | Reword M1 exit gate: `answer a needs-input banner in two taps with the app in foreground or within the OS background grace window; suspended-app banners are out of scope until the push row reopens`. Add under M1 contents: `heartbeat 15 s with two missed probes as dead; background grace suspends rather than tears down; reconnect on foreground with `after_revision` replay; connection log persisted`. Change the push "reopen when" cell to `M1 ships; the gateway sends content-free pings and the phone fetches the body over the E2EE channel`. | major |
| 25 | Phases W0, T0 | Phase S-B (in flight) edits `HelloParams`, `rpc/mod.rs`, `rpc/auth.rs`, `answer.rs` and `hangar-client`; W0 and R1 edit the same files, and D15's per-drain apply lives in `ainb-core`'s `Versioned` sections which P1 moves. T0's `agent_status` section would be section 20 in a `[u64; 19]` array while P1 moves `state.rs` | Split W0 in the phases table: `\| W0-wire \| protocol integer + capability strings + skew harness; op-id ledger and receipts \| S-B merged; spike 4 number in hand \| ... \|` `\| W0-mirror \| section naming + per-drain apply + subscription filter, in `ainb-app` \| P1 merged \| ... \|` Add sequencing rule: ``HelloParams` reaches its final shape once, in W0-wire, after S-B: `{ token, surface, protocol, capabilities, device? }`; R1 adds nothing to hello.` Split T0: `T0-daemon (store, normalizers, OSC schema, RPC) alongside P1; T0-section (`agent_status` as section 20 in `ainb-app`) after P1; the T0 exit gate is met through the existing fleet panel over RPC before the section exists.` | critical |
| 26 | Phases R1 "absorbs D4" | D4 in the base spec is `host switcher + ssh forward, updater, release matrix`. Folding all of D4 into R1 strands the updater and release matrix behind federation | Reword: `R1 absorbs D4's host switcher and transport; D4 keeps the updater and release matrix in the desktop track and lands after D3`. Update the Paint-into-corner row `D4 rescope` accordingly. | minor |
| 27 | Edge cases, Phases R1 | Reconnect storms and partial listings are under-specified: a laptop waking dials N hosts at once, a box restarting is dialed by M devices at once, and the census only names the row-cap case | Add under R1 contents: `per-host reconnect with jittered exponential backoff 1 s to 60 s, at most 2 concurrent resyncs per client, resync order by last-active host`. Replace the census edge case with: `\| listing across hosts \| census per host: `covered \| omitted_by_cap \| unreachable{since} \| stale{head_revision, since}`; the UI never merges a stale host's rows into a fresh list without the badge \|` | major |
| 28 | Security requirements, Components | No observability, retention or logging rule for the new state: device registry, op-id ledger, status events, emulator memory | Add under Security requirements: `- Never log tokens, invite secrets, pairing offers, Noise keys or terminal bytes; log digests only.` Add a section "Operability": `- `hangar/health` gains counters: connections by scope and transport, resyncs and `SnapshotReset` by reason, `data_gap` by reason, ops by disposition, receipts in `unknown`, tier-0 silence seconds per session (p50, max), emulator RSS total.` `- Retention from day one: ledger 7 d, status events 30 d or the existing `fleet_retention` byte ceiling, device registry unbounded but `last_seen_at` shown, revoked rows kept 90 d for audit.` | major |
| 29 | Edge cases | Missing rows for the failures the phases introduce | Add rows: `\| device revoked while its box is offline \| revocation is a box-local write; the token is unusable until the box is up, then rejected at hello; expiry is the backstop; `admin` scope on any paired device may revoke \|` `\| box reinstalled with the same hostname \| new `host_id` and static key; old pairing shows "identity changed, re-pair"; old rows stay under the old host until removed \|` `\| host removed in the desktop with live sessions \| desktop forgets the pairing and asks the box to revoke its own device; the box and its agents are untouched \|` `\| clock skew between phone and box \| pairing TTL uses 30 s leeway; op ids carry no timestamp; status decay uses `mirrored_received_at` \|` `\| hangar-client fresh connection per call over WS \| not allowed off-box: one persistent Noise session per host multiplexes requests and subscriptions; the fresh-dial path stays local-only \|` | major |
| 30 | Spikes | Spike 3 measures reconnect but not throughput; the 50 MB WS terminal gate must hold through `ssh -L` plus Noise, and nothing measures a wake-up storm | Rewrite spike 3: `\| 3 \| peer WS + Noise IK over tailnet and over `ssh -L`; daemon keeps working after desktop closes; reconnect and resync measured; `cat` 50 MB through the terminal stream under both carriers against the 2 s gate; laptop sleep/wake against 5 hosts x 3 devices with the connection count and resync count recorded \| 2-3 days \| R1, D4 rescope \| go/no-go for R1 scope; a throughput miss moves the terminal stream to a second WS without Noise inside `ssh -L` \|` | major |

## Additional spikes

| # | What | Time | Gates | Flips |
|---|---|---|---|---|
| 5 | Noise IK handshake and AEAD framing in the Expo app through a uniffi-compiled Rust crate on a real iPhone and a real Android device; measure cold-start cost and bundle size; confirm keychain custody of the device static key | 2 days | M1, and the `ainb-wire-mobile` component | if uniffi is unworkable, D13 needs a JS Noise implementation review before M1, or the phone speaks TLS 1.3 with a pinned host cert as a second transport kind |
| 6 | iOS and Android background socket lifetime with a 15 s heartbeat: how long a live socket survives suspension, whether a needs-input banner scheduled locally fires after 1, 5 and 30 minutes | 1 day | M1 exit gate wording | if grace is under 60 s, the push reopen row moves to M1+1 rather than "when users ask" |
| 7 | tmux control mode flow control: one pane floods 100 MB/s while a second pane is idle; confirm `pause-after` pauses only the flooding pane, the daemon's RSS stays bounded, and `%continue` resumes without loss | 0.5 day | R2 | if control mode cannot pause per pane, per-viewer batching must drop at the daemon and `data_gap` becomes routine under load |
| 8 | SQLite write path at scale: 100 sessions, hook rate 10/s, status store apply + ledger insert + outbox append in one transaction; measure p99 commit latency and WAL growth over one hour with retention running | 1 day | T0, W0 | if p99 exceeds 50 ms the status store needs a write batch (one transaction per drain tick), not one per event |

## Gates that are not testable as written

| Phase | As written | Testable rewrite |
|---|---|---|
| W0 exit | every mutation carries an op id | a proto test walks a committed `MUTATING_METHODS` list and asserts each params struct embeds `MutationEnvelope`; a daemon test replays a fixture of every mutating method twice and asserts one `created` and one `replayed` per method |
| T0 exit | one status truth across TUI, web, CLI | one fixture session driven hook -> store; `ainb fleet needs --format json`, `GET /api/needs`, and the TUI fleet panel `TestBackend` snapshot produce identical `(host_id, session_key, state, provenance, tier, evidence_observed_at)` tuples |
| T0 exit | no tier-5 row outranks a hook | table test: apply a tier-0 `waiting` then a tier-5 `idle` from the same pane; the row stays `waiting` with provenance `hook`; the reverse order flips to `waiting` |
| T0 exit | silence never `done` | property test over random event sequences: no sequence that ends in silence for any duration yields `done`; only a tier 0/1 `done` or process proof yields `done` or `exited` |
| R1 exit | laptop closed, agents keep running | integration: start a session, kill the client mid-turn, wait 60 s, reconnect with `after_revision`, assert `Complete` replay and the session's transcript advanced |
| R1 exit | revoke one device kills its socket | integration: two devices connected; revoke one; assert its socket closes with 4403 within 1 s, its next hello is refused, the other device's subscription is undisturbed |
| R2 exit | phone-shaped first frame with colour, cursor, alt-screen | snapshot diff: emulator snapshot at 40x20 vs a direct PTY driving the same fixture (alt-screen TUI, OSC 8 link, wide chars) is byte-equal after ANSI normalisation |
| R2 exit | two viewers type without interleaving | two daemon-mediated viewers send 1,000-byte runs concurrently; the PTY receives each run contiguous; the loser's bytes are rejected with `floor_denied` and never reach the pane |
| R2 exit | phone at 40x20 beside desktop without thrash | resize counter on the pane stays at or below 2 per subscription change over a 60 s co-view; the desktop's geometry is restored within 250 ms of the phone detaching |
| M1 exit | answer a needs-input banner in two taps | Maestro or Detox flow against a fixture daemon: banner tap, answer tap; assert the attention row is `answered` with `answered_by = device:<id>` and the receipt is `delivered`; foreground only per amendment 24 |
