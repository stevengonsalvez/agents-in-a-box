# Proof run

**Binary:** `ainb 1.28.2 (1785167a9, 2026-09-19, source)`

**Source:** `1785167a933459cdb1a5f2ac71c7670b3032525d` (the checkout run.sh ran from)

**Result:** 19 of 21 nodes pass, 2 skipped.

| node | result | expected | observed | captures |
|---|---|---|---|---|
| `phase1-keymap` | PASS | with [session_list] attach = "o", keymap list prints attach -> o, enter no longer attaches and o does; in the interactive embed ctrl+c reaches the agent without quitting the TUI and ctrl+q releases; the help overlay agrees with keymap list on n, a, f, F, g and p | ok: help overlay and keymap list agree: n -> new_session (New session)<br>ok: help overlay and keymap list agree: a -> attach_tmux (Attach)<br>ok: help overlay and keymap list agree: f -> refresh (Refresh workspaces)<br>ok: help overlay and keymap list agree: F -> cycle_filter (Cycle session filter)<br>ok: help overlay and keymap list agree: g -> git (Show git view)<br>ok: help overlay and keymap list agree: p -> quick_commit (Commit)<br>known drift outside the spot check: overlay 'd Daemons', keymap 'd delete' (lane H, PR #964)<br>keymap list: attach -> o<br>ok: keymap list prints exactly attach -> o<br>writable tmux clients on the fixture 4 s after enter: 0<br>ok: enter no longer attaches<br>ok: o attaches (a writable tmux client appears on the fixture session)<br>ok: A enters the interactive embed<br>ok: ctrl+c reaches the fixture agent<br>ok: the TUI survives ctrl+c (same pid 1747510)<br>ok: ctrl+q releases back to the preview strip<br>ok: no INTERACTIVE marker remains | `after-detach.txt`, `after-enter.txt`, `after-o-attached.txt`, `clients-after-enter.txt`, `clients-after-o.txt`, `daemon-log-tail.txt`, `help-overlay.txt`, `interactive-after-ctrl-c.txt`, `interactive.txt`, `keymap-attach.txt`, `keymap-list.txt`, `released.txt`, `session-list.txt` |
| `s-a-config-and-headroom` | PASS | two running TUIs each change a different setting through the Config screen and both values survive a restart; typing in config search narrows the matches to the typed key; with a headroom session live in TUI A, TUI B leaves the proxy pid unchanged, logs no spawn, and quitting B leaves A's proxy running | before: branch_prefix=agents/, scan_max_depth=3<br>ok: TUI A saves workspace_defaults.branch_prefix = proofa/<br>ok: TUI B, still holding its startup snapshot, saves workspace_defaults.scan_max_depth = 4<br>config search 'branch_prefix': 1 match(es)<br>ok: config search narrows to the typed key (1 to 3 matches, first row is the key)<br>after restart: branch_prefix=proofa/, scan_max_depth=4<br>ok: A's setting survived B's write and a restart<br>ok: B's setting survived too<br>ok: the restarted Config screen shows A's value<br>ok: the fixture session is marked headroom_enabled in the session store<br>ok: TUI A's watchdog starts the proxy (pid file within 30 s)<br>ok: the proxy answers /health on its port<br>proxy pid with A running: 1750420<br>proxy pid 25 s into B: 1750420; B's log: agents-in-a-box-20260919-155905.jsonl<br>ok: the pid is unchanged while B runs<br>ok: B's log records no headroom spawn<br>listeners on the headroom port: 1<br>ok: exactly one headroom proxy listens on its port<br>proxy pid 12 s after B quit: 1750420<br>ok: quitting B leaves A's proxy alive (same pid, still healthy)<br>ok: B's log records no SIGTERM to the proxy | `a-saved.txt`, `b-saved.txt`, `config-toml-after-both.txt`, `daemon-log-tail.txt`, `headroom-listeners.txt`, `restart-branch-prefix.txt`, `search-branch_prefix.txt`, `search-scan_max_depth.txt`, `tui-b-headroom-log.txt` |
| `s-b-connections` | PASS | with one TUI, ainb web and the listing CLI connected, hangar connections list shows exactly one tui row (the TUI's pid), one web row and one cli row | most tui rows at the TUI's pid across 15 one-second samples: 1<br>rows: tui=1 (at TUI pid 1755526: 1), web=1, cli=1<br>ok: every row carries a pid and the daemon host<br>ok: exactly one web row<br>ok: exactly one cli row (the listing itself)<br>ok: exactly one tui row, at the TUI's pid, in the final listing<br>ok: never more than one tui row at the TUI's pid in any sample | `connections-list.txt`, `daemon-log-tail.txt`, `tui-home.txt`, `tui-row-samples.txt`, `web-log.txt` |
| `s-c-answered` | PASS | an ASK answered from the web retires the TUI control-center card at once with 'answered by web@<host>'; an ASK answered in the TUI shows 'answered by tui@<host>' and the web's later POST /api/answer returns already_answered by tui | web on 46593 (port)<br>web snapshot listed the fixture session after 2s (#1055)<br>ok: the web lists the new session within 10 s<br>ok: the TUI control center shows the web-bound card<br>web snapshot card for 'answered from the web' after 0s (#1055 web lag): att:proof-sc-web:a1f0ee78-d0bc-4952-8271-549de07594d0<br>ok: the web lists the web-bound card within 10 s<br>POST /api/answer (web): {"outcome":"delivered","via":"tmux (tmux_repo-13c852d0)"}<br>ok: the web answer is delivered<br>ok: the TUI card retires within 2 s<br>ok: the TUI title names the web as the answerer<br>ok: the TUI shows the second card<br>web snapshot card for 'answered in the TUI' after 1s (#1055 web lag): att:proof-sc-tui:410a10d2-fcbb-4eaf-a370-54537a6fbfda<br>ok: the web lists the TUI-bound card within 10 s<br>ok: the TUI title names the TUI as the answerer<br>POST /api/answer (web, after the TUI answered): {"outcome":"already_answered","by":"tui@<host>"}<br>ok: the web is told already_answered by tui@ | `control-after-tui-answer.txt`, `control-after-web-answer.txt`, `control-before-tui-answer.txt`, `control-before-web-answer.txt`, `daemon-log-tail.txt`, `palette-miss-control-1.txt`, `web-log.txt` |
| `phase2-sections` | PASS | with no key pressed, the live preview repaints newer agent output and a hook-raised ASK moves an open Fleet panel from 0 to 1 needs input (the daemon's Fleet generation bump reaches the section); a refreshed session list shows a CLI-created session | ok: the session list shows one workspace<br>newest tick on screen: 4, then 9 five seconds later with no key<br>ok: the preview repaints newer agent output with no key pressed<br>session rows 10 s after a CLI ainb run, no key: repo (1) (the list rescans disk on f or its own interval)<br>ok: after f the session list shows both sessions<br>Fleet panel needs input before the ASK: 0<br>raised an ASK through the hook; no key sent to the TUI<br>ok: the Fleet panel repaints to 1 needs input within 20 s, no key pressed<br>ok: it started from 0 | `daemon-log-tail.txt`, `fleet-after.txt`, `fleet-before.txt`, `sessions-one.txt`, `sessions-two.txt` |
| `phase3-uistate` | PASS | a single click on a home sidebar item selects it and a double click opens it; a click on the session list legend collapses it and a click on the collapsed hint restores it; shift+up on a live preview says it has no scrollback without moving the session cursor off row 2, and esc does not quit | ok: a single click moves the sidebar selection to Stats<br>ok: a single click does not open Stats<br>ok: a double click opens Stats, as enter would<br>ok: a click on the legend collapses it<br>ok: a click on the collapsed hint row restores it<br>ok: shift+up on a live preview says it has no scrollback<br>ok: the session cursor stays on row 2 (shift+up is not previous)<br>ok: esc does not quit the TUI<br>G6 step 8's scrollback now needs A (interactive): live previews are read-only since 91c28f712 | `after-esc.txt`, `daemon-log-tail.txt`, `legend-collapsed.txt`, `legend-restored.txt`, `shift-up.txt`, `sidebar-clicked.txt`, `sidebar-double-clicked.txt` |
| `s-d-surfaces` | PASS | scripts/surface-combo-smoke.sh passes all four combinations ({tui} {web} {tui,web} {tui,tui}) on this binary, and two concurrent POST /api/answer calls for one ASK yield exactly one delivered and one already_answered | surface-combo-smoke exit 0: surface-combo-smoke: all four combinations clean <br>ok: the surface-combination smoke exits 0<br>ok: the smoke ran {tui}<br>ok: the smoke ran {web}<br>ok: the smoke ran {tui, web}<br>ok: the smoke ran {tui, tui}<br>web snapshot caught up after 3s<br>web snapshot card for 'Proof S-D' after 2s (#1055 web lag): att:proof-sd:717b2b4e-eb39-43e1-a98e-d24fca19b1fb<br>race replies: {"outcome":"already_answered","by":"web@<host>"}{"outcome":"delivered","via":"tmux (tmux_repo-feda878e)"}<br>ok: exactly one concurrent answer is delivered<br>ok: the other is told already_answered | `answer-race.txt`, `daemon-log-tail.txt`, `surface-combo-smoke.txt`, `web-log.txt` |
| `p1-app-extraction` | PASS | with [session_list] learnings = "L", L opens Learnings and m no longer does (Intent::Key through the table); :recall from the session list opens Learnings (Intent::Command by name); a click on a session row selects it (a pointer intent with no chord) | ok: keymap list binds learnings to L<br>ok: the pointer command select_row is a registry row with no chord<br>ok: m no longer opens Learnings<br>ok: L opens Learnings<br>ok: :recall opens Learnings by command name<br>second session row on screen: 8<br>ok: a click on row 2 selects it | `after-L.txt`, `after-click.txt`, `after-m.txt`, `after-recall.txt`, `daemon-log-tail.txt`, `keymap-session-list.txt` |
| `p2-effects` | PASS | a (AttachTerminal Session) suspends the TUI into a writable tmux client on the fixture and ctrl+b d resumes the session list; A (AttachTerminal InPlace) shows the agent's ticks in the embed and ctrl+q (Detach) returns the strip; o (OpenEditor) with no editor configured says how to set one | ok: a opens a writable tmux client on the fixture session<br>ok: the pane now shows the agent full screen<br>ok: ctrl+b d resumes the TUI's session list<br>ok: the same TUI process resumed (pid 1769482)<br>writable clients after detach: 0<br>ok: A opens the in-place embed<br>ok: the embed shows the fixture agent's ticks<br>ok: ctrl+q detaches back to the preview strip<br>ok: o with no editor configured says how to set one<br>editor notice: ✗ ❌ No editor found. Set preferred editor in settings or───── | `attached-full-screen.txt`, `daemon-log-tail.txt`, `detached.txt`, `editor-notice.txt`, `in-place.txt`, `resumed.txt` |
| `p3-hangar-host` | PASS | ctrl+p fleet then ctrl+p control switch the Hangar plugin's screen by name; on the Daemons screen, start on the headroom proxy row runs the verb and the row reports 'headroom proxy started' with a pid that answers /health | ok: ctrl+p fleet reaches the Fleet screen<br>ok: ctrl+p control reaches the Control Center<br>ok: d opens the Daemons screen<br>ok: the cursor reaches the headroom proxy row<br>ok: Enter offers the verbs<br>ok: the row reports the verb's result<br>proxy pid file: 1772189; row: headroom proxy process ● running 1772189<br>ok: the row's pid is the proxy's pid<br>ok: that proxy answers /health | `daemon-log-tail.txt`, `daemons-after.txt`, `daemons-before.txt`, `daemons-verbs.txt`, `hangar-control.txt`, `hangar-fleet.txt` |
| `p4-review-screens` | PASS | g opens the Review tab listing the worktree's changed files and a click on the second file selects it and shows its diff; ainb diff-review opens the same Code Review surface directly; A shows the agent's ticks in place and ctrl+q returns the strip; :recall opens Learnings from the session list | ok: g opens the Review tab<br>ok: the tab strip names Review, Commits and Markdown<br>ok: a click on SECOND.md selects that row<br>ok: the diff pane follows the selection<br>ok: A opens the in-place embed<br>ok: the embed shows the agent's ticks<br>ok: ctrl+q returns the preview strip<br>ok: :recall opens Learnings from the session list<br>ok: ainb diff-review opens the Code Review surface | `daemon-log-tail.txt`, `diff-review.txt`, `in-place.txt`, `recall.txt`, `review-clicked.txt`, `review-open.txt` |
| `w0-wire` | PASS | a separate ainb CLI call reaches the TUI's own daemon (its own cli row beside the TUI row), and that daemon negotiates auth/hello: protocol 1 selected with a capability catalogue, protocol 99 refused with -32007 | daemon pid from a CLI process: 1775906<br>ok: the daemon a CLI call finds is running<br>ok: that daemon was started by the TUI (its parent chain holds the TUI pid)<br>cli rows seen by the listing: 1; tui rows at pid 1775896: 1<br>ok: the listing sees its own cli row<br>ok: the listing sees the TUI's row on the same daemon<br>hello {1,1}: {"protocol":{"min":1,"max":1},"selected":1,"capabilities":"67 strings","daemon_version":"1.28.2"}<br>hello {99,99}: {"code":-32007,"message":"daemon protocol 1-1 cannot serve a client speaking 99-99; restart from the newer binary"}<br>ok: a client speaking protocol 1 is served with selected=1 and a capability catalogue<br>ok: a client speaking only protocol 99 is refused with -32007 PROTOCOL_INCOMPATIBLE | `daemon-log-tail.txt`, `daemon-status.txt`, `hello-probe.txt` |
| `t0-daemon` | PASS | after a hook-raised ASK, ainb fleet needs, the daemon's fleet/status reply and the TUI Fleet panel all report the fixture session as waiting · hook · tier 0 | ok: ainb fleet needs lists the session within 15 s<br>fleet needs tuple: {"state":"waiting","source":"hook","tier":0}<br>fleet/status tuple: {"state":"waiting","provenance":"hook","tier":"hook","has_open_request":true,"wait_kind":"ask"}<br>Fleet panel detail line: waiting · hook · tier 0 · 30s<br>ok: the CLI read says waiting, hook, tier 0<br>ok: the daemon's fleet/status row says waiting, provenance hook, with an open ask<br>ok: the Fleet panel renders the same state, provenance and tier<br>ok: ainb list still knows the session the hook named by cwd | `daemon-log-tail.txt`, `fleet-needs.txt`, `fleet-panel.txt`, `palette-miss-fleet-1.txt` |
| `issue-963-presence` | PASS | a TUI parked on the home screen is listed as a tui row with its own pid at once, and the row is gone after it quits | ok: hangar daemon setup starts a daemon before the TUI<br>TUI pid 1778841, left on the home screen<br>ok: a tui row with the TUI's pid appears within 5 s<br>tui rows at pid 1778841 while running: 1<br>ok: the TUI process exits on ctrl+c<br>ok: the daemon is still up to answer the listing<br>ok: no tui row names the TUI's pid within 10 s of quitting | `connections-after-quit.txt`, `connections-while-running.txt`, `daemon-log-tail.txt`, `home-screen.txt` |
| `issue-962-fleet-panel` | PASS | with two sessions holding a hook-raised ASK and one with no hook, the Fleet panel's needs-input and all counts equal the waiting rows and total rows the daemon's fleet/status reply carries, and every card's state matches its row | fixtures: two ASK sessions (proof-962-a, proof-962-b) and tmux_repo-1d3e11aa with no hook<br>ok: the daemon holds both ASK sessions within 15 s<br>fleet/status: 2 rows, 2 waiting, 0 idle, 0 working<br>panel lenses: needs input 2, idle 0, running 0, all 2<br>ok: panel needs-input count equals the daemon's waiting rows<br>ok: panel all count equals the daemon's row count<br>ok: panel idle count equals the daemon's idle rows<br>ok: panel running count equals the daemon's working rows<br>ok: two waiting cards carry the daemon's word: waiting · ask | `daemon-log-tail.txt`, `fleet-needs.txt`, `fleet-panel-all.txt` |
| `t0-section` | PASS | a fixture session with a hook-raised ASK renders waiting · hook · tier 0 · Ns (a real age in seconds) in the Fleet panel, and once the daemon is stopped the panel says so on screen: offline, states unverifiable, host unreachable | Fleet panel detail line 5 s after the read: waiting · hook · tier 0 · 10s<br>ok: the panel renders the tuple waiting · hook · tier 0<br>ok: the tuple's age is a real duration (Ns, Nm or Nh), not ?<br>daemon after stop: hangar daemon: stopped (no pid file)<br>ok: the panel header says the daemon is offline<br>ok: the lens body says states unverifiable<br>ok: the lens body names the unreachable host and why<br>the card still reads: waiting · hook · tier 0 · 12s | `daemon-log-tail.txt`, `daemon-stop.txt`, `fleet-daemon-stopped.txt`, `fleet-live.txt`, `fleet-needs.txt` |
| `issue-983-redaction` | PASS | ainb doctor --wire-shape reports no drift from the committed fixture, a token-shaped string in a session label never appears in hangar connections list, the wire-shape frame output, or the web snapshot frame, an ASK whose question carries the token reaches the web snapshot as a card with no cwd and no token, and no absolute path appears anywhere in the web snapshot | doctor --wire-shape exit status: 0<br>ok: doctor --wire-shape exits 0 (no drift)<br>ok: doctor --wire-shape lists no added or removed path<br>label exit: Session label set: tmux_repo-a6597dba -> deploy <canary><br>ok: the token never appears in hangar connections list<br>ok: the token never appears in the wire-shape frame output<br>recorded Claude turn under .claude/projects/-tmp-ainb-proof-run-ty7PbJ-ainb-proof-issue-983-redaction-iQELaT-home--agents-in-a-box-worktrees-by-name-repo--ainb-session-a6597dba--a6597dba<br>fleet cost sessions with a cwd: 0<br>web snapshot caught up after 0s<br>the ASK card reached the web snapshot after 0s<br>operator's own ainb list carries the label: 1 line(s)<br>ok: the token never appears in the web snapshot frame<br>fleet cost saw no recorded turn in this world; the cost panel checks are skipped<br>web snapshot needs cards: 1<br>ok: the canary-bearing ASK reaches the web snapshot as a card<br>ok: no web needs card carries a cwd<br>ok: the fixture session's directory never appears in the web needs cards<br>strings in the web snapshot that are absolute paths: 0<br>ok: no string in the web snapshot is an absolute path<br>ok: the proof world's directory never appears in the web snapshot<br>the canary is written as <canary> in the published captures | `connections-json.txt`, `connections-list.txt`, `daemon-log-tail.txt`, `fleet-cost.txt`, `fleet-needs.txt`, `list-json.txt`, `web-log.txt`, `wire-shape-json.txt`, `wire-shape.txt` |
| `issue-1094-own-session` | PASS | a TUI started inside tmux session own-tui on the private -L proof server lists own-tui and a neighbour under Other tmux; selecting own-tui shows 'This is the tmux session ainb is running in' with the session name, opens no preview client on own-tui and keeps the TUI alive; selecting the neighbour previews its live output through a read-only client | TUI pid 1790339 inside tmux session own-tui<br>ok: the session list shows both sessions under Other tmux<br>ok: own-tui is listed as a row<br>ok: the neighbour previews its live output<br>clients with neighbour selected: own-tui [], neighbour [attached,focused,ignore-size,read-only,UTF-8]<br>ok: the neighbour preview is a read-only client<br>ok: the own session shows the placeholder<br>ok: the placeholder names the session<br>ok: the placeholder says why there is no preview<br>clients with own-tui selected: own-tui [], neighbour []<br>ok: no preview client is opened on the TUI's own session<br>ok: the neighbour's preview client is released<br>ok: the TUI survives selecting its own session (same pid 1790339) | `daemon-log-tail.txt`, `neighbour-selected.txt`, `own-session-selected.txt`, `session-list.txt` |
| `issue-1173-select-tab` | PASS | a click on the log label of the session tab strip makes log the active tab, and a click on the pal label then makes pal active (session_list.select_tab), as tab already moves the strip off preview | tab strip on row 4; active tab before any input: 'preview'<br>ok: preview is the active tab when the session list opens<br>clicking 'log' at column 79, row 4<br>ok: a click on the log label makes log the active tab<br>active tab after the click: 'log'<br>clicking 'pal' at column 74, row 4<br>ok: a click on the pal label makes pal the active tab<br>active tab after the second click: 'pal'<br>ok: tab moves the active tab off 'pal' (the read sees a change)<br>active tab after tab: 'log' | `daemon-log-tail.txt`, `strip-after-click.txt`, `strip-after-second-click.txt`, `strip-after-tab.txt`, `strip-before.txt` |
| `d1-shell` | SKIP | the desktop window renders the sessions sidebar from framed sections, over the daemon a separate CLI call finds, holding exactly one desktop connection row while it runs and none after it closes, and a session the CLI creates reaches the open sidebar without a restart | SKIPPED: webkit2gtk-4.1 is not installed, so the desktop shell cannot be built on this box |  |
| `d2-board` | SKIP | a question raised by a separate hook process reaches the open desktop window's board as a waiting card, from framed sections, a second surface answers it and the daemon records that surface, a third surface reads the same winner, and the window's waiting column drops the card | SKIPPED: webkit2gtk-4.1 is not installed, so the desktop shell cannot be built on this box |  |
