document_id: claude-code-model-spiral-thinking-leak
extracted_at: "2026-03-16T10:00:00"
entities:
  - name: "extended thinking"
    type: concept
    description: "Claude's internal reasoning process that should be hidden from output"
  - name: "system prompt overload"
    type: pattern
    description: "Excessive system prompt size causing model confusion on ambiguous queries"
  - name: "model deliberation spiral"
    type: error
    description: "Model enters infinite loop generating visible internal reasoning as output"
  - name: "claude-code hooks"
    type: technology
    description: "Hook system in Claude Code that runs scripts on events like PreToolUse, Stop, SessionStart"
  - name: "entire"
    type: tool
    description: "Third-party integration that adds hooks on multiple Claude Code events"
  - name: "SessionStart hook"
    type: function
    description: "Hook that runs on session initialization, can inject content into system prompt"
relationships:
  - source: "extended thinking"
    target: "model deliberation spiral"
    type: caused_by
    description: "Thinking tokens leaking into visible output causes feedback loop"
    strength: 9
  - source: "system prompt overload"
    target: "model deliberation spiral"
    type: caused_by
    description: "Massive system prompt compounds the confusion from leaked thinking"
    strength: 7
  - source: "claude-code hooks"
    target: "system prompt overload"
    type: caused_by
    description: "15+ hooks injecting content per turn inflates system prompt"
    strength: 8
  - source: "entire"
    target: "claude-code hooks"
    type: relates_to
    description: "Entire adds 6 hook points that all inject context"
    strength: 6
