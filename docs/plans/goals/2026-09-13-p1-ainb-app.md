# /goal P1 is merged to v2: the renderer-agnostic state machine lives in a new `ainb-app` crate that depends on neither ratatui nor crossterm, `ainb-core` renders from it through `pub use`, and every moved test runs under `cargo test -p ainb-app`

— CONTEXT —
· Project: agents-in-a-box (ainb) desktop programme, slice 2, phase P1 of the base spec `docs/plans/2026-09-04-desktop-shared-core-spec.md` (sections "Extraction plan" row P1, "Renderer contract", "Testing strategy" row Core). Programme DAG and node table: `docs/plans/2026-09-12-desktop-programme.md`. Decisions D1-D18 in `docs/plans/2026-09-11-multi-surface-decisions-spec.md` are locked; D15 (plan B mirror with four invariants) is implemented later by W0-mirror, which is gated on this phase, so leave room for it: section naming stays as `SectionId`, and nothing in the new crate may assume a ratatui frame.
· Stack: Rust workspace under `ainb-tui/` (33 crates, members listed in `ainb-tui/Cargo.toml`). Today the state machine is `ainb-tui/crates/ainb-core/src/app/`: `state.rs` (15,272 lines), `events.rs` (8,929, 36 `KeyEvent` uses, six files under `app/` import `crossterm`), `state_tests.rs` (4,492), `keymap.rs`, `keymap_defaults.rs`, `keymap_toml.rs`, `sections.rs` (19 `Versioned<Section>` from Phase 2, `SectionId::ALL`), `versioned.rs`, `ui_state.rs` (renderer-local, stays in core from Phase 3), `session_loader.rs`, `snapshot.rs`, `registry.rs`, `event_bus.rs`, `attach_handler.rs`, `screens/{mod.rs,builtin.rs}`; 297 `#[test]` functions live under `app/`. `ainb-core/Cargo.toml` depends on `ratatui` and `crossterm` at lines 30-31.
· Current state: slice 1 is complete on `v2` (Phase 1 keymap, S-A, S-B, S-C, Phase 3 UiState seal at PR #945, Phase 2 versioned sections at PR #956, S-D at PR #964). Draw functions take `&AppState` plus `&mut UiState`; zero `Rect` in `state.rs`; `versions()` derives from `SectionId::ALL`; the draw path is compare-then-set. W0-wire (#935) and T0-daemon (#934) are on `v2`; lane C is concurrently working #960/#961 in `ainb-hangar-daemon` and `ainb-hangar-store` on branch `feat/d14-columns`. v2 CI is fully green; registered flakes are #953 (presence e2e) and #958 (acp session cap and two one-off core tests); four macOS tripwires are gated to Linux under #966.
· Working dir: the Orca worktree this session was launched in, branched from `origin/v2`.
· Constraints: PRs target `v2`, never `main`; every commit GPG-signed with `git -c gpg.format=openpgp -c user.signingkey=907EC78C72C6AFF6 commit -S` (the key is unlocked on this box); one concern per commit, named paths, never `git add -A`; no em-dashes anywhere in the diff, including comments and test names; never name any third-party product as prior art in docs; you own `ainb-core/src/app/*`, the new crate `ainb-tui/crates/ainb-app`, `ainb-core/Cargo.toml`, `ainb-tui/Cargo.toml` members, and compile-only fixes in crates that consume the moved types (`ainb-plugin-hangar`, `ainb-web`, `ainb-cli`); never touch `ainb-hangar-daemon`, `ainb-hangar-store`, `ainb-hangar-proto`, or `ainb-fleet-core` (lane C and the wire contract own them); do not start P2 (screen `*State` structs cut at their first render fn stay in core for now); de-crossterm at the host edge: `KeyEvent` becomes a crate-owned `Chord` (modifiers plus key) converted in `ainb-core` before dispatch, and `events.rs` handler signatures take `Chord`; `Intent` per the renderer contract (`Key(Chord) | Command(CommandId, Args) | Mouse(Pos, Btn) | Text(String)`) is the public entry, `AppEvent` stays internal; `ainb-app` gets `serde` on `AppState` and every section (specta stays behind a `typescript-bindings` feature that is off by default); `ainb-core` does `pub use ainb_app::*` so no downstream path changes beyond the crate boundary; keep the Phase 3 seal (no `&mut AppState` in draw fns, zero `Rect` in state) and the Phase 2 invariants (every write bumps exactly its section, `versions()` derived); `docs/tui/keyboard-shortcuts.md` stays byte-identical (the generator runs in CI); tripwires stay in `ainb-core` and must stay green; run `cargo test -p ainb-app -p ainb`, `cargo check --workspace --all-targets`, `cargo clippy --workspace -- -D warnings`, and `cargo fmt --all -- --check` before each push; update the P1 row in the programme doc in the PR that flips it; the two flake issues are rerun, never widened.
· Audience: the desktop app (D1-D3, Tauri host) and the web dashboard, which will render from `ainb-app` sections; W0-mirror and T0-section, which add a per-drain apply path and section 20 on top of this crate.

— SUCCESS CRITERIA (ALL MUST BE TRUE) —
1. `ainb-tui/crates/ainb-app/Cargo.toml` lists neither `ratatui` nor `crossterm` (a test in `ainb-app/tests/` reads the manifest and asserts it, and `cargo tree -p ainb-app -e normal` shows neither), `ainb-app` exposes `AppState`, `Intent`, `Chord`, `dispatch`, `SectionId`, `Versioned`, keymap resolution and the command registry, and `ainb-core` compiles with `pub use ainb_app::*` and no remaining `crossterm::event::KeyEvent` in any handler signature under what used to be `app/events.rs`.
2. `cargo test -p ainb-app` runs at least the 297 tests that lived under `ainb-core/src/app/` (count them before and after; the PR body prints both numbers), plus new behavioural tests: an `Intent::Key(Chord)` resolves through the merged keymap to the same `AppEvent` the old `KeyEvent` path produced for every row in `keymap_defaults.rs`, and a `Text` intent and a `Command` intent each mutate exactly one section version.
3. CI on the final head is fully green on both platforms apart from the registered flakes: every tripwire that ran before still runs and passes, `CLI reference freshness` and the keyboard-shortcuts generator report no diff, and `ainb-web` and `ainb-plugin-hangar` build against the re-export with no behaviour change (their tests pass unchanged).
4. Final deliverable runs without errors
5. You can show proof (screenshot · test output · URL)

— OPERATING RULES — NON-NEGOTIABLE —
1. PLAN FIRST. Output a numbered task list before writing any code.
2. WORK AUTONOMOUSLY. Don't ask clarifying Qs unless genuinely blocked.
3. SELF-VERIFY. After every step: run tests, inspect output, confirm it worked.
4. DEBUG YOURSELF. If it fails, diagnose + fix. Don't hand it back.
5. USE EVERY TOOL. MCPs · terminal · web · code exec · pull real data.
6. NO PLACEHOLDERS. No TODOs · no stubs · real components + real states.
7. PROGRESS LOG. Track completed · in-flight · decisions · blockers.
8. STAY ON GOAL. Discoveries off-spec? Note + keep moving.
9. IF BLOCKED. Log the wall · continue everything parallelizable.
10. CHECK SUCCESS BEFORE STOPPING. Re-read criteria · confirm each is met.

— QUALITY BAR —
· Code: clean, typed, follows project conventions
· Design: looks like a well-funded startup shipped it
· Output: survives a senior code review
· Docs: every new pattern / env var / decision logged

— FINAL DELIVERABLE —
✅ Confirmation each criterion is satisfied
📂 Every file created / modified
🚀 How to run / test / deploy
📊 Proof (screenshot · test output · URL)
📝 Decisions made + anything to know
⚠️ Known limitations + follow-ups

Begin by outputting your plan. Then execute end-to-end without checking
in until done or genuinely blocked.
