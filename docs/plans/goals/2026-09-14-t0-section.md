# /goal T0-section is on v2: agent status is section 20 of the ainb-app state, populated from one joined daemon read, sufficient alone for every consumer, with the failure story rendered and the cross-surface gate extended to the section

— CONTEXT —
· Project: agents-in-a-box (ainb) desktop programme, slice 4 node T0-section (`docs/plans/2026-09-12-desktop-programme.md`), spec `docs/plans/2026-09-11-multi-surface-decisions-spec.md` D14 (one status truth on the fleet_session family, tier, provenance, three clocks, silence is unverifiable never done) and the T0-section row (agent_status as section 20). Issue #1015 carries the nine criteria from the #1014 design review; they are this goal's success criteria and are repeated below in condensed form.
· Stack: Rust workspace under `ainb-tui/`; `ainb-app` sections (`app/sections.rs`, `Versioned<Section>`, `SectionId::ALL`, `versions()/changed_since()` from P1c, the Effect outbox and report intents from P2 and P3); daemon `ainb-hangar-daemon/src/fleet.rs` (`status_rows` and `snapshot_wire` both read `subscription_projection(pool, 0, 0)`); proto `ainb-hangar-proto/src/agent_status.rs`; the hangar plugin `ainb-plugin-hangar/src/screen/fleet.rs` which after #1014 joins roster and status per session_key in the plugin; the cross-surface gate `ainb-core/tests/status_one_truth.rs` and `ainb-hangar-daemon/tests/status_one_truth.rs`; `fleet needs` and `/api/needs`.
· Current state: #1014 (#962) on v2: the Fleet panel derives state, colour, lens and counts from `fleet/status`, `to_wire_session` is gone, supersede runs inside the apply transaction, the drift assertion is strict, `state_of` needs an open request to say waiting. The join still happens in the plugin, and the panel refines DONE from `TURN_COMPLETE` and the wait kind from `ERROR`, two snapshot fields the status row does not carry. P3 (#1008) put plugin views on `ui.state` and daemon verbs behind `Effect::RunDaemonAction`; W0-mirror (not started, gated on #983) will mirror sections to remote hosts.
· Working dir: the Orca worktree this session was launched in (`status-t0` on claude-gcp), branch `feat/t0-section` from `origin/v2` after #1014 merges.
· Constraints: one PR targeting `v2` (a second small PR for the daemon joined read is allowed if it lands first), one file per commit, commit with `git -c commit.gpgsign=false commit` (the orchestrator re-signs at merge) and say so in the PR body, no attribution trailers; no em-dashes on lines you author; no new store migration unless the row needs a column, and then numbered after the latest on v2; never touch `ainb-core/src/app/*`; in `ainb-app` add the section and its reducer only, no host side effects (the fence in `ainb-app/tests/host_side_effects.rs` must stay exact); no `Serialize` on the section or its row types (#983 rules until the redaction layer exists); coordinate with lane F (P4 on `ainb-app`) by rebasing daily and naming shared files in the PR; gates: `cargo test -p ainb-app -p ainb-core -p ainb-hangar-daemon -p ainb-hangar-proto -p ainb-plugin-hangar`, `cargo clippy --workspace -- -D warnings`, `cargo fmt --all -- --check`, and the two status_one_truth gates extended; update the T0-section row in the programme doc in the PR that flips it.
· Audience: the TUI fleet panel today, the desktop host and the phone through W0-mirror, and `fleet needs` and `/api/needs`, all of which must show one row per agent with the same word.

— SUCCESS CRITERIA (ALL MUST BE TRUE) —
1. One read, one join, in the daemon: a single daemon method returns roster and status joined per session_key (each row carries `host_id`, `turn_complete: bool`, the attention kind as an enum, `has_open_request`, `pane_unbound`, and the read revision), the plugin and section both consume it, and the plugin's own join code is deleted; the daemon read amplification per fleet event per surface is asserted at or below today's two.
2. Section 20 is sufficient alone: the panel built from section 20 only, with the roster section absent, renders the same words, colours, lens membership and counts as the two-source panel; a rendered card contains no token that is neither in `AgentState::as_str()` nor a field on the row (the TestBackend gate asserts it); the section's version bumps on a status-row change and not on a transport heartbeat (test: a heartbeat that alters no row leaves the version untouched).
3. Failure story rendered, not logged: section absent, stale (older than the roster revision), or from an unreachable host renders which of the three it is inside the lens body, never a green nothing-needs-you, never a fallback to a local derivation; a `[fleet.status] legacy_panel = true` flag returns the panel to the pre-section read for one release and the PR names the release that removes it; `fleet needs --format json`, `/api/needs` and the section produce identical `(session_key, state, provenance, tier, evidence_observed_at, host_id)` tuples for one fixture session in the extended gate.
4. Final deliverable runs without errors
5. You can show proof (screenshot · test output · URL)

— OPERATING RULES — NON-NEGOTIABLE —
1. PLAN FIRST. Output a numbered task list before writing any code.
2. WORK AUTONOMOUSLY. Don't ask clarifying Qs unless genuinely blocked.
3. SELF-VERIFY. After every step: run tests, inspect output, confirm it worked.
4. DEBUG YOURSELF. If it fails, diagnose + fix. Don't hand it back.
5. USE EVERY TOOL. MCPs · terminal · web · code exec · pull real data.
6. NO PLACEHOLDERS. No TODOs · no stubs · real components + real states.
7. PROGRESS LOG. Track completed · in-flight · decisions · blockers.
8. STAY ON GOAL. Discoveries off-spec? Note + keep moving.
9. IF BLOCKED. Log the wall · continue everything parallelizable.
10. CHECK SUCCESS BEFORE STOPPING. Re-read criteria · confirm each is met.

— QUALITY BAR —
· Code: clean, typed, follows project conventions
· Design: looks like a well-funded startup shipped it
· Output: survives a senior code review
· Docs: every new pattern / env var / decision logged

— FINAL DELIVERABLE —
✅ Confirmation each criterion is satisfied
📂 Every file created / modified
🚀 How to run / test / deploy
📊 Proof (screenshot · test output · URL)
📝 Decisions made + anything to know
⚠️ Known limitations + follow-ups

Begin by outputting your plan. Then execute end-to-end without checking
in until done or genuinely blocked.
