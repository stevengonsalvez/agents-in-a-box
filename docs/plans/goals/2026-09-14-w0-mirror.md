# /goal W0-mirror is on v2: every section of the ainb-app state can be mirrored to a second renderer as named per-section frames with per-drain apply, scalar root selectors and a subscription filter, addressable off-box, redacted by construction, with the fan-out bench and the TypeScript contract diffed in CI

— CONTEXT —
· Project: agents-in-a-box (ainb) desktop programme, slice 4 node W0-mirror (`docs/plans/2026-09-12-desktop-programme.md`), spec `docs/plans/2026-09-11-multi-surface-decisions-spec.md` D15 (renderer contract, Plan B with four invariants: frames name changed sections; one store transaction per channel drain, effects after commit; root selectors return scalars, never lists or objects; renderers may subscribe to a section subset) and the W0-mirror row (section naming, per-drain apply, scalar root selectors, subscription filter in `ainb-app`; listener census and fan-out bench; `typescript-bindings` feature across the crates in the `Type` closure with `AppState.ts` regenerated and diffed in CI like the `docs/tui/cli.md` freshness gate; `attention_local_since` tuple map key replaced by a named key struct; wire/host split of the five mixed sections `tmux`, `workspace_load`, `new_session`, `logs`, `fleet`). Evidence: `research/2026-09-11_multi-surface_SPIKE-4-specta-fanout.md` (the bench and its ceilings). Gate on v2: #983 closed by PR #1026 (per-section `section_json` seam, name and type deny-lists, canary, value-shaped tripwire, locked key-path fixture). Residuals filed as #1032 and the T0-section reviews (#1019): they are criteria below.
· Stack: Rust workspace under `ainb-tui/`; `ainb-app` (`app/sections.rs` with `Versioned<Section>`, `SectionId::ALL`, `versions()/changed_since()`; `wire/` with `section_json`, `serialize_section`, the redaction thread-local, `tests/state_serde.rs` fixture); P2 and P3's Effect outbox and report intents; P4 in flight on lane F (`ainb-app` reducers); T0-section (#1019) adds section 20 with a host task in `ainb-core`; the hangar daemon is the source of Fleet and status; the desktop host (D1) and the phone (M1) are the consumers, neither exists yet, so the second renderer for this goal is a headless test renderer in `ainb-core/tests` plus `ainb-web` if it is cheap.
· Current state: `v2` after #1026 and #1019. Sections are named and versioned; frames are not yet produced by any host; there is no subscription filter, no scalar root selectors, no fan-out bench in CI, no `typescript-bindings` feature in the tree, `attention_local_since` is a tuple-keyed map, five sections still mix wire and host-only fields behind 29 top-level `serde(skip)`; `host_id` is the constant `local` on every row; card age subtracts a remote evidence clock from local now; nothing stops a direct `serde_json::to_value` on the pub Serialize types outside `serialize_section`.
· Working dir: the Orca worktree this session was launched in (`g6-verify` on claude-gcp), branch `feat/w0-mirror` from `origin/v2` after #1026 merges.
· Constraints: PRs target `v2`, staged as needed (naming and selectors; subscription and drain; bench and bindings), each one file per commit, commit with `git -c commit.gpgsign=false commit` (the orchestrator re-signs at merge) and say so in the PR body, no attribution trailers; no em-dashes on lines you author; never touch `ainb-core/src/app/*`; reducers gain no host side effects (the fence in `ainb-app/tests/host_side_effects.rs` stays exact); every frame goes through `serialize_section` and nothing else: add a guard that fails the build or a test when `serde_json::to_value`, `to_string` or `to_vec` is called on an AppState, section or moved type outside `wire/` (or make the derives private behind the seam); section 20 and any section carrying `cwd`, `current_request` or `display_name` ships a projected view with those off the frame; every row that can come from another host carries `host_id` and the section carries the daemon read revision and a daemon read clock so a consumer never subtracts a remote clock from its own; the `attention_local_since` key becomes a named struct; the wire/host split moves host-only fields out of the five mixed sections into `UiState` or host structs rather than adding skips; the `typescript-bindings` feature is one workspace feature with `AppState.ts` committed and a CI job that regenerates and diffs it; the fan-out bench runs in CI on a fixed input and asserts the ceilings; coordinate daily with lane F (P4, `ainb-app` reducers) and lane C (#1031 single owner for section 20) by rebasing and naming shared files; gates: `cargo test -p ainb-app -p ainb-core -p ainb-hangar-proto`, `cargo clippy --workspace -- -D warnings`, `cargo fmt --all -- --check`; update the W0-mirror row in the programme doc in the PR that flips it.
· Audience: the desktop host lane (D1) that consumes these frames next, the phone companion, the security reviewer who checks that no frame can carry a secret by construction, and Stevie.

— SUCCESS CRITERIA (ALL MUST BE TRUE) —
1. Contract: a headless second renderer in `ainb-core/tests` subscribes to a section subset, receives frames that name only changed sections (each frame carries the section id, version, host_id and daemon read revision plus read clock where the section has one), applies them with one store transaction per channel drain with effects after commit, and reads root selectors that return only scalars (a test fails if any root selector returns a list or an object); the same renderer with the hot section unsubscribed applies no frame for it; `attention_local_since` is keyed by a named struct and the five mixed sections have no host-only field left behind a top-level skip.
2. Safety by construction: every emitted frame passes the #1026 deny-lists, canary and tripwire; a guard fails when any code outside `wire/` serialises an AppState, section or moved type; section 20's frame carries no `cwd`, `current_request` or `display_name`; `host_id` is real on every mirrored row (a two-host fixture folds without collision) and card age never subtracts a remote clock from local now (a fixture with a 90 s skew renders the daemon's age).
3. Bench and bindings in CI: the fan-out bench at 100 sessions and 2,000 frames in 2 s runs in a CI job and asserts at most 12,800 computation runs, at most 32.5 ms apply per 1,000 frames, longest apply unit at most 2.4 ms and apply units exactly ceil(burst_ms / drain_ms) (125 at 16 ms), with per-send mode failing both numeric gates; the `typescript-bindings` feature builds across the `Type` closure and a CI job regenerates `AppState.ts` and fails on any diff from the committed file; the programme doc W0-mirror row flips with the run ids.
4. Final deliverable runs without errors
5. You can show proof (screenshot · test output · URL)

— OPERATING RULES — NON-NEGOTIABLE —
1. PLAN FIRST. Output a numbered task list before writing any code.
2. WORK AUTONOMOUSLY. Don't ask clarifying Qs unless genuinely blocked.
3. SELF-VERIFY. After every step: run tests, inspect output, confirm it worked.
4. DEBUG YOURSELF. If it fails, diagnose + fix. Don't hand it back.
5. USE EVERY TOOL. MCPs · terminal · web · code exec · pull real data.
6. NO PLACEHOLDERS. No TODOs · no stubs · real components + real states.
7. PROGRESS LOG. Track completed · in-flight · decisions · blockers.
8. STAY ON GOAL. Discoveries off-spec? Note + keep moving.
9. IF BLOCKED. Log the wall · continue everything parallelizable.
10. CHECK SUCCESS BEFORE STOPPING. Re-read criteria · confirm each is met.

— QUALITY BAR —
· Code: clean, typed, follows project conventions
· Design: looks like a well-funded startup shipped it
· Output: survives a senior code review
· Docs: every new pattern / env var / decision logged

— FINAL DELIVERABLE —
✅ Confirmation each criterion is satisfied
📂 Every file created / modified
🚀 How to run / test / deploy
📊 Proof (screenshot · test output · URL)
📝 Decisions made + anything to know
⚠️ Known limitations + follow-ups

Begin by outputting your plan. Then execute end-to-end without checking
in until done or genuinely blocked.
