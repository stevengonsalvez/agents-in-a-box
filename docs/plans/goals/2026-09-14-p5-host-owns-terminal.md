# /goal P5 is merged to v2 and slice 2 is closed: the host owns the terminal client and the plugin runtime handle, every store the reducer writes goes through the persistence effect, the remaining screen states live in `ainb-app`, and D1 can start against a seam with no PTY, no `.save()` and no runtime handle reachable from the state machine

— CONTEXT —
· Project: agents-in-a-box (ainb) desktop programme, slice 2, phase P5 of the base spec `docs/plans/2026-09-04-desktop-shared-core-spec.md` ("Extraction plan" row P5: `inbox`, `session_recovery`, `skill_manager_screen`, `log_history_viewer`, `config` state; "Renderer contract" rows `Effect` and `Intent`). Programme DAG: `docs/plans/2026-09-12-desktop-programme.md`. Decisions D1-D18 in `docs/plans/2026-09-11-multi-surface-decisions-spec.md` are locked. The success criteria below are issue #1035 (the design seam check of P4) plus the P5 spec row plus one P4 residual; items 1 to 4 of #1035 are the ones D1 is blocked by.
· Stack: Rust workspace under `ainb-tui/`. The state machine is `ainb-tui/crates/ainb-app` (P1 to P4 on v2: PRs #973, #975, #982, #1000, #1008, #1021). Side effects are `Effect` values the host runs after each drain (`ainb-app/src/app/effect.rs`, `ainb-core/src/effect_host.rs`); host reports come back as intents; plugin actions and `ui.state` ride protocol 0.1.2 with a per-plugin topic; the wire seam is `ainb-app/src/wire/` with the key-path fixture `tests/fixtures/section_key_paths.txt` (#1026) and `ainb doctor --wire-shape`. `host_side_effects.rs` is the fence test that names what a state-machine path can still reach.
· Current state on v2 at ffeb666c (2026-09-14): P4 merged with eight review fixes; `AppState.tmux.embed` still holds an `EmbedClient` through `LocalEmbed` and a process-global registry; `sync_terminal_observer` is still on `AppState`; `portable-pty` is still listed in `REACHABLE_TODAY`; the reducer still calls `.save()` on `app_config`, `favorites_store`, `session_label_store`, the session-meta store and the onboarding config; `plugins_host.plugin_runtime` is still a runtime handle in state; `watch_screen` carries no viewport and the tick's fallback size is hardcoded (`state.rs:13194`); the command gate does not resolve through the key gate's precedence; `TmuxSessionName::new` rejects sigils and control characters but still accepts leading and trailing whitespace (`ainb-app/src/app/effect.rs:103`); tripwire exclusions are per binary; #1023, #1024, #1025, #1027, #1028 are red tripwires with no verdict; `PLUGIN_SCREENS` is a static list while `inbox` and `fleet_panel` now produce `ui.state`. W0-mirror (lane K, PR #1036) diffs sections for the desktop mirror and needs the granularity decision in item 9 and the `HostFlags` decision in item 6. Lane C (#1031, PR #1038) owns section 20 and the fleet plugin's roster reads; #1040 (a TUI listed twice as `tui`) is theirs after that.
· Working dir: the Orca worktree this session runs in (`p1-ainb-app`), branched from `origin/v2` on a new branch `feat/p5-host-owns-terminal`. Run `cargo clean` in `ainb-tui/` before the first build of this goal (the box has 150 GB and the previous target reached 40 GB), then build with `-j 4`.
· Constraints: PRs target `v2`, never `main`; every commit GPG-signed with `git -c gpg.format=openpgp -c user.signingkey=907EC78C72C6AFF6 commit -S`; one file per commit, one concern per commit, named paths, never `git add -A`; never mention any AI tool or assistant in a commit message and never add attribution trailers; no em-dashes on any line you author; never name a third-party product as prior art; push after every commit; you own `ainb-app`, `ainb-core/src/effect_host.rs`, `ainb-core/src/main.rs`, `ainb-core/src/app/*` remnants and compile-only fixes in `ainb-plugin-hangar`, `ainb-web`, `ainb-cli`; never touch `ainb-hangar-daemon`, `ainb-hangar-store`, `ainb-hangar-proto`, `ainb-fleet-core`, `ainb-core/src/agent_status_host.rs` (lane C) or `ainb-app/src/wire/` beyond what a moved field forces (lane K owns the mirror; coordinate through the PR, not by editing their files); keep the Phase 3 seal, the Phase 2 invariants, the #1026 fixture (update it in its own commit with the reason in the message when a field moves off state) and `docs/tui/keyboard-shortcuts.md` byte-identical; run `cargo test -p ainb-app -p ainb`, `cargo check --workspace --all-targets`, `cargo clippy --workspace -- -D warnings` and `cargo fmt --all -- --check` before each push; open the PR as a draft, flip it ready only when CI on the head is green apart from the registered flakes (#958, #844 cluster); update the P5 row of the programme doc in the PR that flips it and record every decision this goal asks for in the PR body; when the reviews come back, land each fix as its own one-file commit and reply on the PR with the sha per item.
· Audience: D1 (the desktop shell and sidecar), which must be able to host `ainb-app` with no PTY, no file writes and no runtime handle inside the state machine; W0-mirror, which reads the decisions in items 6 and 9; the web dashboard.

— SUCCESS CRITERIA (ALL MUST BE TRUE) —
1. The host owns the terminal client and the runtime handle. `AppState.tmux.embed` holds no `EmbedClient`; state keeps `embed_session: Option<String>` plus focus and the host keeps the client; `LocalEmbed`, the process-global registry, `enter_interactive_pane` (already gone) and `sync_terminal_observer` are deleted from `ainb-app`; `portable-pty` leaves `REACHABLE_TODAY` in `host_side_effects.rs`, which then guards the seam mechanically; `plugins_host.plugin_runtime` moves to the host and `main.rs` passes the host's own handle. `TerminalTarget::InPlace` either becomes portable through the report or the effect doc and the programme doc say in one line that it is terminal-host-only and what D1 does instead. Tests: `tripwire_interactive_pane` drives the effect and the report; `host_side_effects.rs` names neither a PTY nor a runtime handle reachable from `ainb-app`.
2. Persistence and gating are complete. Every store the reducer writes (`app_config`, `favorites_store`, `session_label_store`, the session-meta store, the onboarding config) goes through the persistence effect, with a `CALL_SITES`-style source guard asserting zero `.save()` on a reducer-reachable path; the command gate resolves through the same precedence as the key gate (test: `git_view.scroll` with a confirmation dialog open changes nothing) and the PR body states whether `HostFlags` moves into `AppState` or intents carry it; `watch_screen` carries `{screen, watching, width, height}` with the largest requested size winning across hosts and no hardcoded fallback (test: two hosts at different sizes); the render tick reads `ui.state` views for every producing plugin, either through `PLUGIN_SCREENS` covering `inbox` and `fleet_panel` or by reading the registry (test: a view from a plugin outside the static list renders); `TmuxSessionName::new` rejects leading and trailing whitespace with one more table case.
3. The remaining screen states from the spec row (`inbox`, `session_recovery`, `skill_manager_screen`, `log_history_viewer`, `config`) live in `ainb-app` with their tests; the tripwire ratchet closes both ways (per-test exclusions, a scheduled job that runs only the excluded set and warns when one goes green, a SKIP guard that asserts the expected set rather than a ratio); #1023, #1024, #1025, #1027 and #1028 each carry a one-sentence verdict (test rot or product bug) with evidence, as a comment on the issue; the PR body records the mirror granularity decision (W0-mirror diffs by key path, or `PluginsHostSection` splits per plugin) with the worst-case bytes per tick; CI on the final head is green on both platforms apart from the registered flakes, and the P5 row in the programme doc reads done with the PR number.
4. Final deliverable runs without errors
5. You can show proof (screenshot · test output · URL)

— OPERATING RULES — NON-NEGOTIABLE —
1. PLAN FIRST. Output a numbered task list before writing any code.
2. WORK AUTONOMOUSLY. Don't ask clarifying Qs unless genuinely blocked.
3. SELF-VERIFY. After every step: run tests, inspect output, confirm it worked.
4. DEBUG YOURSELF. If it fails, diagnose + fix. Don't hand it back.
5. USE EVERY TOOL. MCPs · terminal · web · code exec · pull real data.
6. NO PLACEHOLDERS. No TODOs · no stubs · real components + real states.
7. PROGRESS LOG. Track completed · in-flight · decisions · blockers.
8. STAY ON GOAL. Discoveries off-spec? Note + keep moving.
9. IF BLOCKED. Log the wall · continue everything parallelizable.
10. CHECK SUCCESS BEFORE STOPPING. Re-read criteria · confirm each is met.

— QUALITY BAR —
· Code: clean, typed, follows project conventions
· Design: looks like a well-funded startup shipped it
· Output: survives a senior code review
· Docs: every new pattern / env var / decision logged

— FINAL DELIVERABLE —
✅ Confirmation each criterion is satisfied
📂 Every file created / modified
🚀 How to run / test / deploy
📊 Proof (screenshot · test output · URL)
📝 Decisions made + anything to know
⚠️ Known limitations + follow-ups

Begin by outputting your plan. Then execute end-to-end without checking
in until done or genuinely blocked.
