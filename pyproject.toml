# agents-in-a-box — workspace root for Python projects in this monorepo.
# Currently houses one workspace member: reflect-kb (Python library +
# `reflect` CLI). Additional packages can be added later by extending
# the workspace.members list.

[tool.uv.workspace]
members = ["reflect-kb"]
